version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env
    ports:
      - '8000:8000'
    environment:
      - PYTHONUNBUFFERED=1

  # frontend is served by backend StaticFiles; remove separate frontend service
